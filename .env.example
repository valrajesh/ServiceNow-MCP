# Copy to .env and fill in your ServiceNow credentials.
# Never commit .env.

# Local dev only. In App Service, the platform-provided PORT takes precedence.
MANAGEMENT_PORT=3001
ENABLED_MCP_SERVERS=incident-mcp,cmdb_server,server_mcp

SN_INSTANCE_URL=https://cgsb.service-now.com/login.do
SN_USERNAME=akt2tjb
SN_PASSWORD=Nova@123
